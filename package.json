{
  "name": "ikigaro-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "build": "echo 'Static landing page - no build step needed.'",
    "deploy": "npx wrangler deploy"
  }
}
